#define rEFInd_DEBUG (0)


VOID
DebugLog (
  IN        INTN  DebugMode,
  IN  CONST CHAR8 *FormatString, ...
);

#if rEFInd_DEBUG == 0
  #define MsgLog(...)
#else
  #define MsgLog(...)  DebugLog(rEFInd_DEBUG, __VA_ARGS__)
#endif

